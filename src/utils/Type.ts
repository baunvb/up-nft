export type Nft = {
    address: string,
    price: number,
    image: string,
    title?: string,
    description?: string,
    series_number?: number,
    series?: number,
    vr_id?: string,
    contract?: string
}
